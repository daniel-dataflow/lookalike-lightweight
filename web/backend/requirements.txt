# ============================================================
# Lookalike Lightweight - FastAPI Backend (Render 배포용)
# 설치: pip install -r requirements.txt
# ============================================================

# FastAPI 프레임워크
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0
jinja2==3.1.3

# 인증
bcrypt==4.1.2
httpx==0.28.1
email-validator==2.3.0

# 데이터베이스 (Supabase PostgreSQL + pgvector)
psycopg2-binary==2.9.9
pgvector==0.3.6
sqlalchemy==2.0.25

# Supabase SDK (Storage 등)
supabase==2.13.0

# 에러 트래킹 (Render 배포용)
sentry-sdk[fastapi]==2.1.1

# AI/ML 외부 API
google-genai==1.14.0

# 이미지 처리
Pillow>=10.0.0

# 기타
urllib3<2

gradio_client
